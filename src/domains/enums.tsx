export enum GraphicType {
    Wall,
    Finish,
    Robot,
}

export enum Directions {
    Up,
    Right,
    Down,
    Left,
}

export enum MoveCommand {
    Go,
    Left,
    Right
}

export enum GameStatus {
    Running,
    Stopped
}